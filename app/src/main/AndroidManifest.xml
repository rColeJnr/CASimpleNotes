<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rick.casimplenotes">

    <application
        android:name=".SimpleNote"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_note"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_note_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CASimpleNotes">
        <activity
            android:name=".feature_note.presentation.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.CASimpleNotes.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>